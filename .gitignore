# Xcode
.DS_Store
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/

# Swift Package Manager
.build/
.swiftpm/

# Model files (too large for git)
*.gguf
OnDeviceCatalyst/qwen2.5-3b-instruct-q4_k_m.gguf
Sources/OnDeviceCatalyst/qwen2.5-3b-instruct-q4_k_m.gguf

# XCFramework binaries (distributed via GitHub releases)
llama.xcframework/
llama.xcframework.zip
*.xcframework/
*.xcframework.zip

# Build artifacts
DerivedData/
build/

# IDE
.vscode/
.idea/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Temporary files
*.tmp
*.temp

# Private research and agent working directory.
# The private research track lives in a SEPARATE PRIVATE repository, cloned to
# .context/research for local work. It must never be committed here.
# This was previously protected only by a machine-local .git/info/exclude rule,
# which does not travel to clones. Tracked so every contributor is protected.
.context/
