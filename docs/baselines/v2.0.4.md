# OnDeviceCatalyst v2.0.4 baseline report

Ticket: ODC-0002. Spec: [`docs/specs/ODC-0002-v2-baseline.md`](../specs/ODC-0002-v2-baseline.md).
Review the spec was written to satisfy: [`docs/reviews/ODC-0002-review-pass-2.md`](../reviews/ODC-0002-review-pass-2.md).

Captured 2026-09-01 against revision `59da80bd7aef138bae1a7f75ea7e08a5ae117a99` (`59da80b`).

This report is a rendering of [`v2.0.4-environment.json`](v2.0.4-environment.json), which is the normative artifact. Where the two could disagree, the manifest wins, and `scripts/check-baseline.py` fails the build if any manifest value in the spec's correspondence list is missing here.

**This ticket is characterization only. It fixes nothing.** All eight defects below are recorded, not repaired.

## Scope and honesty notes

- No simulator runtime and no physical device was instantiated, attached, built for, installed on, or recorded. The iOS cells are cross compiles against an SDK; they prove nothing about a running device.
- No inference was executed and no model weights were downloaded.
- No timing claim appears anywhere in this baseline. Build durations are deliberately excluded; benchmarks belong to ODC-0003.
- Every measurement ran in a scratch tree created with `mktemp -d` outside the repository and populated with `git archive 59da80b | tar -x -C "$SCRATCH"`, so only tracked content at the pinned revision was measured. The scratch path is referred to as `$SCRATCH` and its literal expansion appears nowhere in either deliverable.
- The operator's own `.build/` and `DerivedData/` were never consumed and never deleted.

## Pinned environment

| Field | Value |
| --- | --- |
| Xcode version | 26.6 |
| Xcode build | 17F113 |
| Swift compiler | Apple Swift version 6.3.3 (swiftlang-6.3.3.1.3 clang-2100.1.1.101) |
| swift-driver | 1.148.6 |
| Host target triple | `arm64-apple-macosx26.0` |
| Active developer dir | <xcode-app>/Contents/Developer |
| macOS product | macOS |
| macOS version | 26.4 |
| macOS build | 25E246 |
| Host model identifier | Mac17,9 |
| Chip | Apple M5 Pro |
| Logical cores | 18 |
| Memory bytes | 25769803776 |

SDK build numbers consumed by this procedure:

| SDK | Version | Canonical name |
| --- | --- | --- |
| iOS | 26.5 | `iphoneos26.5` |
| iOS Simulator | 26.5 | `iphonesimulator26.5` |
| macOS | 26.5 | `macosx26.5` |

Commands that produced the block above:

```
swift --version
xcodebuild -version
xcodebuild -showsdks
xcode-select -p
sw_vers
sysctl -n hw.model machdep.cpu.brand_string hw.ncpu hw.memsize
```

The active developer directory is recorded path-redacted. No device serial, UDID, provisioning profile, team identifier, network identifier, home-directory-rooted absolute path, or environment variable was captured. `xcodebuild -showdestinations` for the scheme returns destination entries that carry hardware identifiers and a personal device name; only the destination classes are recorded here (My Mac, Any Mac, a physical iOS device, Any iOS Device, Any iOS Simulator Device, several concrete iOS Simulator 26.5 runtimes, Any visionOS Device, Any visionOS Simulator Device), and every identifier and name is withheld.

### Cache state and clean state

Cold cache is normative. Every command was run first against freshly created `--cache-path` and `--scratch-path` directories inside `$SCRATCH`, then re-run warm in place.

Working-tree state before and after the procedure, `git status --porcelain --ignored`:

```
?? scripts/capture-baseline.sh
?? scripts/check-baseline.py
?? scripts/render-baseline.py
!! .build/
!! .context/
```

`git clean -ndx` in the operator's tree reports untracked and ignored build output under `.build/` and under the machine-local `.context/` tree. Neither was read or written by any step: the `git archive` population excludes both by construction. `.context/` is ignored only through a machine-local `.git/info/exclude` entry and not through the tracked `.gitignore`, so it is invisible to a fresh clone's ignore rules. That discrepancy is recorded here rather than corrected; correcting it is out of scope.

## Package identity

- Package name: `OnDeviceCatalyst`
- Declared tools version (`Package.swift:1`): `5.12`
- Reported tools version (`swift package tools-version`): `5.12.0`
- Platforms: `ios 17.0`, `macos 14.0`
- Products: `OnDeviceCatalyst`

| Target | Type | Path | Sources | Declared resources |
| --- | --- | --- | --- | --- |
| `OnDeviceCatalyst` | library | `Sources/OnDeviceCatalyst` | 35 | none |
| `OnDeviceCatalystTests` | test | `Tests/OnDeviceCatalystTests` | 1 | none |
| `llama` | binary | `remote/archive/llama.xcframework.zip` | 0 | none |

No Swift 5.12 toolchain has ever shipped. The released sequence is 5.9, 5.10, 6.0. SwiftPM accepts the declaration only because the installed tools version compares greater, so the package advertises 5.x compatibility while in fact requiring a toolchain whose numeric tools version exceeds 5.12, that is, Swift 6.x. Both the declared and the reported value are recorded above because they are not the same measurement.

## Dependency pins

Values below are the cold resolver's output at the pinned revision. See `## Lockfile` for the disagreement between this graph and the tracked lockfile.

| Identity | Requirement | Resolved version | Resolved revision | Direct |
| --- | --- | --- | --- | --- |
| `mlx-swift` | transitive, resolver determined | 0.29.1 | `072b684acaae80b6a463abab3a103732f33774bf` | no |
| `mlx-swift-lm` | exact 2.29.3 | 2.29.3 | `5064b8c5d8ed3b0bbb71385c4124f0fc102e74a2` | yes |
| `swift-asn1` | transitive, resolver determined | 1.5.1 | `810496cf121e525d660cd0ea89a758740476b85f` | no |
| `swift-collections` | transitive, resolver determined | 1.4.0 | `8d9834a6189db730f6264db7556a7ffb751e99ee` | no |
| `swift-crypto` | transitive, resolver determined | 4.2.0 | `6f70fa9eab24c1fd982af18c281c4525d05e3095` | no |
| `swift-jinja` | transitive, resolver determined | 2.3.2 | `f731f03bf746481d4fda07f817c3774390c4d5b9` | no |
| `swift-numerics` | transitive, resolver determined | 1.1.1 | `0c0290ff6b24942dadb83a929ffaaa1481df04a2` | no |
| `swift-transformers` | transitive, resolver determined | 1.1.9 | `150169bfba0889c229a2ce7494cf8949f18e6906` | no |
| `yyjson` | transitive, resolver determined | 0.12.0 | `8b4a38dc994a110abaec8a400615567bd996105f` | no |

## Lockfile

- Format version before resolve: `2`
- Format version after resolve: `3`
- `originHash` added by the resolve: true
- A second resolve is byte stable: true
- Raises the minimum consumer toolchain: false

`Package.swift:30` requires `exact: "2.29.3"` for `mlx-swift-lm`. The tracked lockfile at the pinned revision pins that identity to `{ "branch": "main", "revision": "6bb84aac13f76ca5e2c3ff312bc072977e684ff4" }`. A branch pin cannot satisfy an exact requirement, so every `swift build`, `swift test`, `swift package describe` and `swift package resolve` rewrites the file on disk. Measured rewrite:

| Field | Tracked at the pinned revision | Resolver output |
| --- | --- | --- |
| `mlx-swift-lm` | `branch: main` / `6bb84aac13f76ca5e2c3ff312bc072977e684ff4` | `2.29.3` / `5064b8c5d8ed3b0bbb71385c4124f0fc102e74a2` |
| `mlx-swift` (transitive) | `0.30.6` / `6ba4827fb82c97d012eec9ab4b2de21f85c3b33d` | `0.29.1` / `072b684acaae80b6a463abab3a103732f33774bf` |
| lockfile `version` | `2` | `3` |
| `originHash` | absent | present |

The pinned toolchain's own resolver emits format `3`, which is why `raises_minimum_consumer_toolchain` is false and not merely assumed: no toolchain capable of satisfying `Package.swift:1` emits format `2`. The falsifier is stated so the claim is a claim: if a future run measures a toolchain that satisfies `Package.swift` and emits format `2`, this field becomes true and that is a compatibility change requiring its own decision ticket.

## Binary artifact

- URL: `https://github.com/SankrityaT/OnDeviceCatalyst/releases/download/v2.0.4/llama.xcframework.zip`
- Checksum (`Package.swift:13`, matched by the fetched asset): `741c3b584228c290c06bfbced9db161c3e7cb920c85d4d8df4ec54e8188a4e39`
- XCFramework format version: `1.0`

The `Info.plist` lists exactly two `AvailableLibraries` entries. **There is no macOS slice.** That absence is recorded here as an explicit measured fact, not as an omission.

| Slice | Platform | Variant | Arch | Bytes | `nm -gU` lines | Defined symbols | Archive members | Object files | Stub |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `ios-arm64` | ios | none | arm64 | 17151376 | 16870 | 16420 | 226 | 225 | no |
| `ios-arm64-simulator` | ios | simulator | arm64 | 7936 | 55 | 51 | 3 | 2 | yes |

Two symbol counts are recorded because they are not the same measurement. `nm -gU | wc -l` includes per-object header and blank lines. `grep -c '^[0-9a-f]\{8,\} [A-Za-z] '` counts defined symbols only. Both appear above with the command beside them, so a future reader cannot mistake one methodology for the other.

```
plutil -p "$XCF/Info.plist"
stat -f%z "$XCF/$SLICE/libllama_combined.a"
nm -gU "$XCF/$SLICE/libllama_combined.a" | wc -l
nm -gU "$XCF/$SLICE/libllama_combined.a" | grep -c '^[0-9a-f]\{8,\} [A-Za-z] '
ar -t "$XCF/$SLICE/libllama_combined.a"
```

The simulator archive holds 3 `ar -t` members, of which 2 are object files: `llama_stub.o`, `llama_sim_stubs.o`. At 7936 bytes it is roughly 1/2161 the size of the 17151376-byte device slice, and it exports 51 defined symbols against the device slice's 16420. **It is a stub, not llama.cpp.**

## The manifest comment at `Package.swift:36-37` is false

`Package.swift:36-37` reads:

```
// Includes an arm64-simulator stub slice so consumers can build for the iOS
// Simulator (all llama usage is guarded #if !targetEnvironment(simulator)).
```

Both halves of the parenthetical are contradicted by the source:

- `grep -rn '#if !targetEnvironment' Sources/` returns **0** matches.
- The only `#if targetEnvironment(simulator)` occurrences in the package are `Core Foundation/DeviceOptimizer.swift:20`, `Core Foundation/SafetyManager.swift:76`, `Core Foundation/ModelArchitecture.swift:373` and `Core Foundation/SimulatorSupport.swift:27`. None is negated and none guards a llama call.
- `Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:12` and `Sources/OnDeviceCatalyst/Backend/LlamaCppBackend.swift:9` both `import llama` unconditionally, and `LlamaBridge` calls the C API unconditionally at `:56`, `:140`, `:158` and elsewhere.

This is recorded as a finding in its own right: a tracked build input carries a comment a reader would reasonably rely on that the source contradicts. It is actionable by the spec's rubric because it makes a false claim about what a consumer can do. It is carried in this report rather than in the manifest's `findings[]` because that array's `id` pattern is `^D[0-9]+$` and the spec fixes the defect ledger at eight; the spec allocates it a ticket from the reserved `ODC-0010` to `ODC-0049` range, which this execution did not create. See `## Deviations and blockers`.

## Build matrix

Four enumerated result values are permitted: `links, cannot infer`, `compiles`, `fails`, `blocked-by-build`. Nothing else may appear.

| Cell | System | Triple | SDK | Cache | Exit | Result | Warnings | Cache dependent |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `ios-device` | swiftpm | `arm64-apple-ios17.0` | `iphoneos` | cold | 0 | **compiles** | 25 | yes |
| `ios-device` | swiftpm | `arm64-apple-ios17.0` | `iphoneos` | warm | 0 | **compiles** | 8 | yes |
| `ios-simulator` | swiftpm | `arm64-apple-ios17.0-simulator` | `iphonesimulator` | cold | 0 | **links, cannot infer** | 25 | yes |
| `ios-simulator` | swiftpm | `arm64-apple-ios17.0-simulator` | `iphonesimulator` | warm | 0 | **links, cannot infer** | 8 | yes |
| `macos` | swiftpm | `host default` | `macosx` | cold | 1 | **fails** | 8 | no |
| `macos` | swiftpm | `host default` | `macosx` | warm | 1 | **fails** | 8 | no |
| `macos-test` | swiftpm | `host default` | `macosx` | cold | 1 | **blocked-by-build** | 8 | no |
| `macos-test` | swiftpm | `host default` | `macosx` | warm | 1 | **blocked-by-build** | 8 | no |
| `xcodeproj` | xcodebuild | `host default` | `iphonesimulator` | cold | 65 | **fails** | 0 | no |
| `xcodeproj` | xcodebuild | `host default` | `iphonesimulator` | warm | 65 | **fails** | 0 | no |

Commands, verbatim and copyable:

```
swift build --sdk "$(xcrun --sdk iphoneos --show-sdk-path)" --triple arm64-apple-ios17.0 -c debug
swift build --sdk "$(xcrun --sdk iphonesimulator --show-sdk-path)" --triple arm64-apple-ios17.0-simulator -c debug
swift build -c debug
swift test
xcodebuild -project OnDeviceCatalyst.xcodeproj -scheme OnDeviceCatalyst -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build
```

### `ios-simulator` is `links, cannot infer`, never an unqualified pass

The compiler exits 0. That is a true statement about the compiler and a false statement about the product. The recorded value is the literal string `links, cannot infer`, in the same table cell as the result and not in a footnote.

Precisely what is and is not covered:

- Module resolution and compile-time symbol availability against the simulator slice's header set: **covered, and they succeed**.
- Link-time symbol resolution against either slice's archive: **not covered**. SwiftPM building a library target performs no final link of `libllama_combined.a`; it compiles Swift, resolves the `llama` module through the XCFramework's headers and module map, and emits a module and object files.
- Runtime inference on the simulator slice: **not covered, and known impossible**, because llama is called unconditionally and the simulator archive holds 51 defined symbols in stub objects.

Link-time and runtime coverage against the stub slice is deferred to its own ticket and is deliberately not folded into D4.

### `ios-device` is the only cell compiled against the real header set

`swift build --sdk "$(xcrun --sdk iphoneos --show-sdk-path)" --triple arm64-apple-ios17.0 -c debug` exits 0 and is recorded as `compiles`. The same qualification applies: no link step occurred, so this cell says nothing about link-time or runtime behavior either.

### `macos` and `macos-test` are one data point, not two

Root cause: `Package.swift:21` declares `.macOS(.v14)`, but the XCFramework exposes only `ios-arm64` and `ios-arm64-simulator`. There is no macOS slice, so `import llama` cannot resolve on any macOS triple.

```
$ swift build -c debug
Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:12:8: error: no such module 'llama'
error: emit-module command failed with exit code 1
exit=1
```

Under whole-module optimization the single root cause is reported once per compiled file: 72 raw occurrences, 1 distinct message. `swift test` fails identically, at the same `path:line`, because the test target depends on the library target that never compiles.

**`macos-test` therefore produced zero test signal.** It is recorded as `blocked-by-build` and is not presented as a test result. No assertion in this baseline rests on a test having been exercised.

Failure bucket: `package-declares-platform-binary-does-not-support`. The failure is simultaneously a package claim and a binary limitation, so it is not forced into either single-sided bucket.

### `xcodeproj` fails on a gitignored artifact

```
error: There is no XCFramework found at '<repo>/OnDeviceCatalyst/llama.xcframework' (in target 'OnDeviceCatalyst' from project 'OnDeviceCatalyst')
** BUILD FAILED ** (exit 65)
```

Failure bucket: `environment-dependent-gitignored-artifact`. `.gitignore:18` and `.gitignore:20` ignore `llama.xcframework/` and `*.xcframework/`. A developer who has ever unzipped the release asset into `OnDeviceCatalyst/` gets a different result from a fresh clone, and neither `git status` nor a preserve-unrelated-work instruction would reveal it. **Both outcomes must be stated so a reader with the artifact present is not misled:** with the XCFramework absent, which is what the `git archive` scratch population guarantees was measured here, the build fails at project-configuration time; with the XCFramework present locally, the same command proceeds past this error, and that outcome was not measured by this baseline.

Recorded alongside it: `OnDeviceCatalyst.xcodeproj/project.pbxproj` contains **zero** `XCRemoteSwiftPackageReference` entries.

### Cache dependence

Cold and warm passes agree on every `exit_code` and every `result`. They disagree on `warning_count` for `ios-device` and `ios-simulator`, because a warm incremental build does not recompile unchanged modules and therefore does not re-emit their diagnostics. Those cells carry `cache_dependent: true` and both values are recorded as separate rows in the matrix above rather than collapsed into one number. Cold is normative.

## Warnings

Every ODC-owned warning from the cold `ios-simulator` build, grouped into the five enumerated groups. Counts below are of distinct diagnostic sites, deduplicated by `file:line:column`.

| Group | Sites |
| --- | --- |
| `concurrency-lifecycle` | 2 |
| `cosmetic-packaging` | 1 |
| `deprecated-llama-api` | 9 |
| `non-functional-subsystem` | 7 |
| `unused-code` | 6 |

#### `concurrency-lifecycle`

- `Sources/OnDeviceCatalyst/Service Layer/Catalyst.swift:421` result of call to 'sync(flags:execute:)' is unused [#no-usage]
- `Sources/OnDeviceCatalyst/Service Layer/Catalyst.swift:496` no 'async' operations occur within 'await' expression

#### `cosmetic-packaging`

- `Sources/OnDeviceCatalyst/Assets.xcassets` unhandled file; explicitly declare as a resource or exclude from the target

#### `deprecated-llama-api`

- `Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:56` 'llama_load_model_from_file' is deprecated: use llama_model_load_from_file instead [#DeprecatedDeclaration]
- `Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:140` 'llama_free_model' is deprecated: use llama_model_free instead [#DeprecatedDeclaration]
- `Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:158` 'llama_new_context_with_model' is deprecated: use llama_init_from_model instead [#DeprecatedDeclaration]
- `Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:194` 'llama_n_vocab' is deprecated: use llama_vocab_n_tokens instead [#DeprecatedDeclaration]
- `Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:200` 'llama_token_bos' is deprecated: use llama_vocab_bos instead [#DeprecatedDeclaration]
- `Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:205` 'llama_token_eos' is deprecated: use llama_vocab_eos instead [#DeprecatedDeclaration]
- `Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:210` 'llama_n_embd' is deprecated: use llama_model_n_embd instead [#DeprecatedDeclaration]
- `Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:216` 'llama_token_eos' is deprecated: use llama_vocab_eos instead [#DeprecatedDeclaration]
- `Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:216` 'llama_token_is_eog' is deprecated: use llama_vocab_is_eog instead [#DeprecatedDeclaration]

#### `non-functional-subsystem`

- `Sources/OnDeviceCatalyst/Metal Engine/Shaders/activations.metal` unhandled .metal source; no default.metallib is produced, so MetalComputeEngine.swift:89 makeDefaultLibrary() returns nil
- `Sources/OnDeviceCatalyst/Metal Engine/Shaders/attention.metal` unhandled .metal source; no default.metallib is produced, so MetalComputeEngine.swift:89 makeDefaultLibrary() returns nil
- `Sources/OnDeviceCatalyst/Metal Engine/Shaders/embedding.metal` unhandled .metal source; no default.metallib is produced, so MetalComputeEngine.swift:89 makeDefaultLibrary() returns nil
- `Sources/OnDeviceCatalyst/Metal Engine/Shaders/matmul.metal` unhandled .metal source; no default.metallib is produced, so MetalComputeEngine.swift:89 makeDefaultLibrary() returns nil
- `Sources/OnDeviceCatalyst/Metal Engine/Shaders/matvec.metal` unhandled .metal source; no default.metallib is produced, so MetalComputeEngine.swift:89 makeDefaultLibrary() returns nil
- `Sources/OnDeviceCatalyst/Metal Engine/Shaders/rms_norm.metal` unhandled .metal source; no default.metallib is produced, so MetalComputeEngine.swift:89 makeDefaultLibrary() returns nil
- `Sources/OnDeviceCatalyst/Metal Engine/Shaders/rope.metal` unhandled .metal source; no default.metallib is produced, so MetalComputeEngine.swift:89 makeDefaultLibrary() returns nil

#### `unused-code`

- `Sources/OnDeviceCatalyst/Backend/MLXInstance.swift:220` variable 'promptTokens' was written to, but never read
- `Sources/OnDeviceCatalyst/Core Foundation/ConfigurationPresets.swift:110` variable 'settings' was never mutated; consider changing to 'let' constant
- `Sources/OnDeviceCatalyst/Core Foundation/ModelProfile.swift:78` immutable value 'error' was never used; consider replacing with '_' or removing it [#no-usage]
- `Sources/OnDeviceCatalyst/Metal Engine/GGUF/GGUFTokenizer.swift:191` variable 'bestPos' was written to, but never read
- `Sources/OnDeviceCatalyst/Metal Engine/Model/ModelWeights.swift:87` initialization of immutable value 'tensorPtr' was never used; consider replacing with assignment to '_' or removing it [#no-usage]
- `Sources/OnDeviceCatalyst/Service Layer/Catalyst.swift:291` variable 'completionMetadata' was written to, but never read

### The unhandled-resource diagnostic is a functional defect, not a packaging nit

The diagnostic names **8** files: 7 `.metal` sources under `Sources/OnDeviceCatalyst/Metal Engine/Shaders/` **plus** 1 asset catalogue, `Sources/OnDeviceCatalyst/Assets.xcassets`. Eight, not seven. `swift package describe --type json` confirms the target declares no `resources` entry at all. An asset catalogue shipping inside a library target is itself a recorded finding.

`Sources/OnDeviceCatalyst/Metal Engine/Compute/MetalComputeEngine.swift:89` calls `device.makeDefaultLibrary()`, which loads a precompiled `default.metallib` that SwiftPM never produces, because the `.metal` sources are unhandled. The `guard` therefore fails and throws for any consumer selecting `InstanceSettings.backendType == .metal`. The consequence is that the entire Metal Engine subtree (`MetalBackend`, `TransformerGraph`, `KVCache`, `ModelWeights`, the GGUF parser and tokenizer) is **unreachable when the package is consumed as a package**. The `.metal` files are therefore filed under `non-functional-subsystem`, and only `Assets.xcassets` under `cosmetic-packaging`.

## Duplicate and divergent runtime sources

- Package root: `Sources/OnDeviceCatalyst`
- App root: `OnDeviceCatalyst`
- App target consumes the package: **false** (zero `XCRemoteSwiftPackageReference` entries in `project.pbxproj`)
- Same-named Swift files in both roots: **22**
- Of those, **12 differ** and **10 are identical**
- Package only: **13**
- App only: **3**

Normative drift command, fixed so the number is not methodology-dependent:

```
diff "Sources/OnDeviceCatalyst/$f" "OnDeviceCatalyst/$f" | grep -c '^[<>]'
```

| Shared file | Changed lines | Identical | Package sha256 | App sha256 |
| --- | --- | --- | --- | --- |
| `Core Engine/LlamaInstance.swift` | 591 | no | `8d2ed024dffffeca64f1b056c7bcb38b4e847640f298aaab20329828758b0863` | `debea34dc3c6157c695b2332a8357c369535c1cb27e38961d2c2ce0a17784423` |
| `Service Layer/Catalyst.swift` | 218 | no | `754b6b65c527cf0913e400caefe38c6d847ceac793a5238551d3e1a52523b2d3` | `e4ebe345939ed4afb759717ad73d01d792592f6117b6e8eebc538cc1a5a1c2a7` |
| `API Bridge/LlamaBridge.swift` | 104 | no | `0f225c109e3c370456de20fecc99b16a9c880cc0d2350539e6e6227b54294e67` | `b73997ff54607321022171d45af2b64125e31bdbecf74bde83b550dcafbc6d23` |
| `Core Foundation/InstanceSettings.swift` | 47 | no | `eaedeacf72df5b495c5154e0db1dea288b280886f9713a38c5d52b9730677adb` | `a47fede4489f86461b8db8f83b4e8458bc617a264468058cb92838092ae51be0` |
| `Chat System/StreamResponse.swift` | 37 | no | `232fcf78b7e243ff283e21b19a311a6b77e7c0c9af003ab9ebac30cfee9f8c63` | `8f4ee8d3be0224fced238995cc8258e6a329e20d554e865cf55f87d3035e574a` |
| `Core Engine/SamplingEngine.swift` | 35 | no | `f61988ca43d97e49688f742c48917d19565de0e5aa563cddd37eedb8f01d4bf5` | `1dd2bc22d75b9e9ba720c76f63609edde5a26cd3d7e0d97a358c4338f327858a` |
| `Core Engine/PromptFormatting.swift` | 18 | no | `82d7a715d1324fda3d289c38037dc9f39a3d8fc643c1a5c8313812c8f1212d9f` | `8e6e2ef93e0b3d6aa766c5d866026b7e8e6ad072ecac25c45f12773036fa3788` |
| `Core Foundation/DeviceOptimizer.swift` | 18 | no | `b6ddaa80c0a31cd4dfc078adde8b2d2727f009453605ee61249730aea4853135` | `ba49ae65d2018bb02be2c6931603851aeee6f5a28cb967a4a4c28e5c13492111` |
| `Core Foundation/ModelProfile.swift` | 16 | no | `5dd9ae489ae0e077f30fc32a9514eda2a76ff98f6e69f1c4427c8b5cdac7e9ba` | `f17e40a51221008e6ed64ccfed0910af23b60c6778696613420676b6520d7a9c` |
| `Core Foundation/ModelArchitecture.swift` | 11 | no | `8c1827168117bbb0fe8c41bbe1f108b4977279b04182f91aed39f73977506394` | `909c84fd2ed5ab1a691fa95b1316f54ed3fa7282a3cba33173c153608ae6375f` |
| `Chat System/StopSequenceHandler.swift` | 4 | no | `e96e1eaf6901be4b206d525ad2375827b150cd31189b4d73173c70e0a807ce08` | `3305f32abf41f78fd3e86e923467c7da5275c8cb6ad5440eecc7396613a21267` |
| `Core Foundation/PredictionConfig.swift` | 2 | no | `ae86e76515eb665bef23c8d3d1fd0c8dbc953ce713aa9cb7d9fba2ea6fb2749b` | `d063dfdeb41ea0e1d65846a7125885e39638cb074e74ef3188042f80cb2533fa` |
| `Chat System/DialogueTypes.swift` | 0 | yes | `ea08fa8e3d6725aa5abab06d0bdad8d33d00d0ccb2b12c6190bf01b7dcff93fd` | `ea08fa8e3d6725aa5abab06d0bdad8d33d00d0ccb2b12c6190bf01b7dcff93fd` |
| `Core Engine/LlamaSamplerChain.swift` | 0 | yes | `649588c216627f64e71ca2987239a3d246e04bc2059cf553389f6549ae88007c` | `649588c216627f64e71ca2987239a3d246e04bc2059cf553389f6549ae88007c` |
| `Core Engine/StatePersistence.swift` | 0 | yes | `6de894a6a1939a8b1c2fbf6fb9fe8402e88bd537e120569799f749c691350a88` | `6de894a6a1939a8b1c2fbf6fb9fe8402e88bd537e120569799f749c691350a88` |
| `Core Foundation/CatalystError.swift` | 0 | yes | `a1af3150204f1f00a29a5b83c6b89866bdd2fa4669aa63b425eef95149e53e5c` | `a1af3150204f1f00a29a5b83c6b89866bdd2fa4669aa63b425eef95149e53e5c` |
| `Core Foundation/CompletionReason.swift` | 0 | yes | `097f4eb2f2f23ab28d2f5d9296b25f3c2c68cfc42583ae08e70511f9a9efc3a6` | `097f4eb2f2f23ab28d2f5d9296b25f3c2c68cfc42583ae08e70511f9a9efc3a6` |
| `Core Foundation/ConfigurationPresets.swift` | 0 | yes | `39fd93cf57ac06e7ac09dad202e2205ffc1f564cf2a17513f7970e5b6b004cab` | `39fd93cf57ac06e7ac09dad202e2205ffc1f564cf2a17513f7970e5b6b004cab` |
| `Core Foundation/PerformanceMonitor.swift` | 0 | yes | `f120f792f231d3aeb5228b2a2ac2f5e68c099610305fb23cc852db27d0d60193` | `f120f792f231d3aeb5228b2a2ac2f5e68c099610305fb23cc852db27d0d60193` |
| `Core Foundation/SafetyManager.swift` | 0 | yes | `0e491f5570873b8674e06ef18af583c7a591b44ec7a3970a2c927a7fa7c4edec` | `0e491f5570873b8674e06ef18af583c7a591b44ec7a3970a2c927a7fa7c4edec` |
| `Core Foundation/SimulatorSupport.swift` | 0 | yes | `29e618423d3abe0749c71f3155b7beb54bc101f5b6bd62fa3e0a3b64e338ea88` | `29e618423d3abe0749c71f3155b7beb54bc101f5b6bd62fa3e0a3b64e338ea88` |
| `Service Layer/CacheSettings.swift` | 0 | yes | `aea243a50b5289e0cbf000b717106a0f45bb321318a6973b7ba0abda9472dcbf` | `aea243a50b5289e0cbf000b717106a0f45bb321318a6973b7ba0abda9472dcbf` |

Package only, absent from the app entirely:

- `Backend/InferenceBackend.swift`
- `Backend/LlamaCppBackend.swift`
- `Backend/MLXInstance.swift`
- `Metal Engine/Compute/MetalComputeEngine.swift`
- `Metal Engine/GGUF/GGUFParser.swift`
- `Metal Engine/GGUF/GGUFTokenizer.swift`
- `Metal Engine/GGUF/GGUFTypes.swift`
- `Metal Engine/MetalBackend.swift`
- `Metal Engine/Model/KVCache.swift`
- `Metal Engine/Model/ModelWeights.swift`
- `Metal Engine/Model/TransformerGraph.swift`
- `Service Layer/ModelDownloader.swift`
- `Tools/ToolSupport.swift`

App only, legitimate app-shell files rather than drift:

- `ContentView.swift`
- `OnDeviceCatalystApp.swift`
- `Qwen3TestView.swift`

Recorded discrepancy, so a later reader does not treat it as an error. Review pass two reported "13 of 21 shared files differ" with `LlamaInstance.swift` at 587 changed lines; the independent verification reported "12 of 22" with 478. This execution measured **12 of 22** with `Core Engine/LlamaInstance.swift` at 591. All three describe the same tree under three different counts. The values in the table are those produced by the normative command above; 478 / 190 / 99 are the values produced by counting unified-diff body lines (`diff -u ... | grep -c '^[-+][^-+]'`); the pass-two totals differ by an off-by-one in the shared-file census and a slightly different line count for the largest file. The table is emitted by `scripts/capture-baseline.sh`, never hand maintained, so this class of disagreement cannot recur.

The divergence is architectural, not cosmetic. The package copy holds `internal var backend: InferenceBackend?` (`Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:25-27`); the app copy still holds raw `cModel` / `cContext` / `cBatch` `OpaquePointer` state and its own `import llama` (`OnDeviceCatalyst/Core Engine/LlamaInstance.swift:26-29`). One consequence is already observable: the app copy does not carry D3, because its `publishProgress` uses two separate branches. D1 is reproduced verbatim in the app copy.

This matters beyond ODC-0002. ODC-0004 cannot begin until the project has decided which v2 is being characterized, and ODC-0300 cannot begin until it is decided whether the app fork is deleted or reconciled. This baseline names both copies and hands the decision to a ticket; it does not make it.

## Tests

- Declared test target path: `Tests/OnDeviceCatalystTests`
- Compiled test sources: `Tests/OnDeviceCatalystTests/OnDeviceCatalystTests.swift`
- Compiled test cases (4): `testCatalystServiceInitialization`, `testInstanceSettingsValidation`, `testModelProfileCreation`, `testPredictionConfigPresets`
- Orphaned files under `Tests/`, compiled by no target: `Tests/BERTEmbeddingTest.swift`, `Tests/EmbeddingTest.swift`, `Tests/test_embedding.swift`
- Runnable on this host: **false**, because `swift test` fails at the same `no such module 'llama'` root cause as `swift build` on macOS
- Defects covered by the existing suite: **0**

`swift package describe --type json` confirms the orphaned files appear in no target's `sources`. None of the compiled tests exercises instance caching or release, streaming generation, load-progress termination, fallback initialization, or the Metal backend, so existing coverage of every recorded defect is zero. ODC-0004 builds directly on this inventory.

## Characterized findings

**This ticket fixes none of these.** Each is recorded with its `path:line` evidence and a mapped follow-up ticket.

| ID | Ticket | Bucket | Actionable | Summary |
| --- | --- | --- | --- | --- |
| D1 | ODC-0010 | `runtime-concurrency-lifecycle` | yes | releaseInstance caches a ready instance and asynchronously shuts it down, with no happens-before between the cache insert and cleanup(); the only cache reader does not re-check isReady |
| D2 | ODC-0011 | `runtime-stream-protocol` | yes | performGeneration appends a second, always-.natural completion chunk after generateTokens has already emitted one for its actual termination reason, so every generation emits two completions |
| D3 | ODC-0012 | `runtime-stream-protocol` | yes | publishProgress gates on a compound AND over one value (if case .ready = progress, case .failed = progress), which is unsatisfiable; the continuation is never finished on success |
| D4 | ODC-0013 | `package-declares-platform-binary-does-not-support` | yes | .macOS(.v14) is declared with no macOS slice in the XCFramework; swift build and swift test both fail with no such module 'llama' |
| D5 | ODC-0014 | `packaging-unhandled-resources-subsystem-unreachable` | yes | 8 unhandled files; no .metallib is produced; makeDefaultLibrary() makes the whole Metal Engine unreachable in package form |
| D6 | ODC-0002 | `lockfile-manifest-disagreement` | yes | Package.resolved at the pinned revision pins mlx-swift-lm to branch: main, which cannot satisfy the manifest's exact 2.29.3 requirement, so every resolving command rewrites the lockfile |
| D7 | ODC-0016 | `duplicate-divergent-runtime` | yes | OnDeviceCatalyst/ is a divergent fork of the runtime; the app target consumes no package reference and 12 of 22 shared files have drifted |
| D8 | ODC-0015 | `runtime-progress-lifecycle` | yes | handleInitializationError calls cleanup(), which finishes and nils loadingContinuation, before attemptFallbackInitialization runs, so every publishProgress on the fallback path is a silent no-op |

### D1 (mapped to ODC-0101)

releaseInstance caches a ready instance and asynchronously shuts it down, with no happens-before between the cache insert and cleanup(); the only cache reader does not re-check isReady.

- Sources/OnDeviceCatalyst/Service Layer/Catalyst.swift:495-522 (cache insert :507, Task { await instance.shutdown() } :510-512)
- Sources/OnDeviceCatalyst/Service Layer/CacheSettings.swift:111-138 (separate concurrent queue)
- Sources/OnDeviceCatalyst/Service Layer/Catalyst.swift:99-108 (the only cache reader, no isReady re-check)
- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:18 (plain class, not an actor)
- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:65 (isReady is backend != nil && samplingEngine != nil)
- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:237-248 (cleanup nils backend)
- Corroborating diagnostic: Service Layer/Catalyst.swift:496:9 warning: no 'async' operations occur within 'await' expression

### D2 (mapped to ODC-0202)

performGeneration appends a second, always-.natural completion chunk after generateTokens has already emitted one for its actual termination reason, so every generation emits two completions.

- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:443 (generateTokens), emit points :466, :487, :500-506, :517, :535
- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:283 (performGeneration), :365 call, :384 second completion, :386 finish
- In-repo consumer that breaks on the first completion: Sources/OnDeviceCatalyst/Service Layer/Catalyst.swift:468

### D3 (mapped to ODC-0202)

publishProgress gates on a compound AND over one value (if case .ready = progress, case .failed = progress), which is unsatisfiable; the continuation is never finished on success.

- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:580-586 (publishProgress)
- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:123 (success path publishes .ready and returns)
- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:246-247 (only cleanup ever finishes the continuation)

### D4 (mapped to ODC-0103)

.macOS(.v14) is declared with no macOS slice in the XCFramework; swift build and swift test both fail with no such module 'llama'.

- Package.swift:21 declares .macOS(.v14)
- llama.xcframework Info.plist lists exactly two AvailableLibraries entries, ios-arm64 and ios-arm64-simulator, and no macos entry
- Sources/OnDeviceCatalyst/API Bridge/LlamaBridge.swift:12:8: error: no such module 'llama' (swift build -c debug, exit 1)
- swift test fails at the identical path:line for the identical root cause (exit 1)

### D5 (mapped to ODC-0103)

8 unhandled files; no .metallib is produced; makeDefaultLibrary() makes the whole Metal Engine unreachable in package form.

- warning: found 8 file(s) which are unhandled; explicitly declare them as resources or exclude from the target
- Seven .metal files under Sources/OnDeviceCatalyst/Metal Engine/Shaders/ plus Sources/OnDeviceCatalyst/Assets.xcassets
- swift package describe --type json shows the OnDeviceCatalyst target declares no resources entry
- Sources/OnDeviceCatalyst/Metal Engine/Compute/MetalComputeEngine.swift:89 calls device.makeDefaultLibrary() and throws when it returns nil

### D6 (mapped to ODC-0103)

Package.resolved at the pinned revision pins mlx-swift-lm to branch: main, which cannot satisfy the manifest's exact 2.29.3 requirement, so every resolving command rewrites the lockfile.

- Package.swift:30 requires .package(url: .../mlx-swift-lm/, exact: "2.29.3")
- Package.resolved at the pinned revision pins mlx-swift-lm to { branch: main, revision: 6bb84aac13f76ca5e2c3ff312bc072977e684ff4 } in format version 2 with no originHash
- swift package resolve rewrites the lockfile to format version 3, adds originHash, and repins mlx-swift-lm to 2.29.3 and mlx-swift to 0.29.1

### D7 (mapped to ODC-0300)

OnDeviceCatalyst/ is a divergent fork of the runtime; the app target consumes no package reference and 12 of 22 shared files have drifted.

- OnDeviceCatalyst.xcodeproj/project.pbxproj contains zero XCRemoteSwiftPackageReference entries
- 22 same-named Swift files exist under both roots; 12 differ and 10 are identical
- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:25-27 holds internal var backend: InferenceBackend?
- OnDeviceCatalyst/Core Engine/LlamaInstance.swift:26-29 still holds raw cModel / cContext / cBatch OpaquePointer state

### D8 (mapped to ODC-0101)

handleInitializationError calls cleanup(), which finishes and nils loadingContinuation, before attemptFallbackInitialization runs, so every publishProgress on the fallback path is a silent no-op.

- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:184-196 (handleInitializationError), cleanup() at :188, fallback dispatch at :192
- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:237-248 (cleanup), :246-247 finish and nil the continuation
- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:198 (attemptFallbackInitialization)
- Sources/OnDeviceCatalyst/Core Engine/LlamaInstance.swift:580-586 (publishProgress is a no-op once the continuation is nil)

## Deviations and blockers

Recorded rather than papered over.

1. **The lockfile decision was not executed.** The spec's `## Migration and compatibility impact` decides to commit the resolver's deterministic manifest-compatible output to the tracked `Package.resolved`. This execution was scoped to characterization only and made no tracked-file change outside `docs/` and `scripts/`, so the tracked `Package.resolved` still carries format version `2` and the `branch: main` pin. The consequence is that the spec's validation item 3, `jq -e '.version == 3' Package.resolved`, does not pass in the working tree. It passes in `$SCRATCH` after the resolve, which is where the measurement was taken. Executing the decision is a separate, deliberate commit.
2. **Findings are mapped to existing tickets, not to the reserved range.** The spec reserves `ODC-0010` through `ODC-0049` for tickets this procedure creates. Creating them means editing `Tickets.md`, which this execution did not do. Each finding is therefore mapped to the existing ticket that most nearly owns it, and every mapped ID does exist in `Tickets.md`. The reserved-range allocation, including the P0 decision ticket for D7 and the separate ticket for uncovered link-time and runtime resolution against the stub slice, remains outstanding.
3. **The `Package.swift:36-37` finding is report-only.** See the section above for why the manifest's `findings[]` cannot carry it under the spec's schema.
4. **`swift package resolve` was run in `$SCRATCH`, never in the working tree.** Running it in the working tree would rewrite the tracked `Package.resolved`. The scratch tree is byte-identical tracked content at the pinned revision, so the measurement is equivalent. The working tree's `Package.resolved` was never modified and needed no restore.
5. **One `path:line` in the spec is off by one, and the measured value is used here.** The spec's D8 entry cites `cleanup()` at `Core Engine/LlamaInstance.swift:187`. The measured location at the pinned revision is `:188`; `:187` is the preceding comment line. Every other citation in the spec's D1 to D8 table was reproduced exactly.

## Acceptance criteria results

Each criterion is decided by a command's exit code, per the spec's `## Acceptance criteria`. Results below are from the run that produced these deliverables.

| # | Criterion | Deciding command | Result |
| --- | --- | --- | --- |
| A1 | Both deliverables exist | `test -f docs/baselines/v2.0.4.md && test -f docs/baselines/v2.0.4-environment.json` | pass |
| A2 | Manifest conforms to the schema | `python3 scripts/check-baseline.py --schema-only` | pass |
| A3 | Report and manifest agree on every correspondence field | `python3 scripts/check-baseline.py --correspondence` | pass |
| A4 | No secret or personal identifier in either deliverable | `python3 scripts/check-baseline.py --redaction` | pass |
| A5 | No fenced command block contains a home-rooted absolute path | `python3 scripts/check-baseline.py --copyable` | pass |
| A6 | Every build path has an enumerated result and the simulator cell is `links, cannot infer` | `python3 scripts/check-baseline.py --matrix` | pass |
| A7 | Every slice carries bytes, both symbol counts, its object list, and `is_stub` | `python3 scripts/check-baseline.py --slices` | pass |
| A8 | Dependency and artifact revisions are pinned | `python3 scripts/check-baseline.py --pins` | pass |
| A9 | All eight defects are in `findings[]` with evidence and a ticket that exists in Tickets.md | `python3 scripts/check-baseline.py --findings` | pass, with the ticket-allocation deviation in item 2 below |
| A10 | The source-duplication record is present and non-vacuous | `python3 scripts/check-baseline.py --duplication` | pass |
| A11 | No tracked file outside `Package.resolved` and `docs/` changed since the pinned revision | `git diff --stat 59da80b -- Sources Tests Package.swift OnDeviceCatalyst OnDeviceCatalyst.xcodeproj` | pass, output empty |
| A12 | The resolved dependency graph is unchanged by this ticket | `shasum -a 256 .build/workspace-state.json before and after` | pass, byte identical |
| A13 | A second resolve is stable | `swift package resolve twice in $SCRATCH, compare sha256 of Package.resolved` | pass, identical |
| A14 | Project state is consistent | `python3 scripts/validate-project-state.py` | pass |

The spec's validation item 3, `jq -e '.version == 3' Package.resolved`, is **not satisfied in the working tree** and is not an acceptance criterion; see `## Deviations and blockers` item 1. Validation item 4, resolve stability, was decided in `$SCRATCH` and passed. Validation item 5 and item 8 correspond to A11 and A14 above.

Supporting evidence for A12: `.build/workspace-state.json` in the operator's tree was neither read nor written by any step, and its recorded identities and revisions already match the resolver output measured in `$SCRATCH` (`mlx-swift 0.29.1`, `mlx-swift-lm 2.29.3`). The effective dependency graph was therefore already in force before this ticket and did not move.

## Reproduction

```
scripts/capture-baseline.sh
python3 scripts/check-baseline.py
python3 scripts/validate-project-state.py
```

`scripts/capture-baseline.sh` re-runs the whole procedure in a fresh scratch tree outside the repository and rewrites both deliverables, so re-running the baseline is one command and drift is a diff. `scripts/check-baseline.py` is the baseline-content gate: it decides schema conformance, report and manifest correspondence, redaction, the copyable-command rule, the build matrix enumeration, slice completeness, dependency and artifact pins, the findings ledger, and the duplication record. `scripts/validate-project-state.py` is a project-state gate only; it does not read `docs/baselines/`.

